name: "[TEST] caller"

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  caller-ref:
    uses: dariocurr/checkout-called/.github/workflows/intermidiate.yml@sha

  caller-sha:
    uses: dariocurr/checkout-called/.github/workflows/intermidiate.yml@461770172bfbf709f72ef3e2e96ab8d67e550ad6
