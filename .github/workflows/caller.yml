name: "[TEST] caller"

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  caller-ref:
    uses: dariocurr/checkout-called/.github/workflows/called.yml@sha

  caller-sha:
    uses: dariocurr/checkout-called/.github/workflows/called.yml@844a01ef312455c723ecef612b98bf775976a1b8
