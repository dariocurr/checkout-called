name: "[TEST] caller"

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  caller-ref:
    uses: dariocurr/checkout-called/.github/workflows/called.yml@sha

  caller-sha:
    uses: dariocurr/checkout-called/.github/workflows/called.yml@cd97959fb18afbd7412e3e0381c03ded895d50fa
