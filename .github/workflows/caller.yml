name: "[internal] caller"

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  validate-ref:
    uses: dariocurr/checkout-called/.github/workflows/validate.yml@dev
    secrets: inherit

  validate-sha:
    uses: dariocurr/checkout-called/.github/workflows/validate.yml@0a8a55e07f415dc9bb64863f0fa5c8ffadf3d933
    secrets: inherit
