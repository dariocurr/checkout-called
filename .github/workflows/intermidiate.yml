name: "[TEST] intermidiate"

on:
  workflow_call:

jobs:
  caller-ref:
    uses: dariocurr/checkout-called/.github/workflows/called.yml@dev

  caller-sha:
    uses: dariocurr/checkout-called/.github/workflows/called.yml@${{ github.sha }}
